# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

**`README.md` is the complete spec** — the what, why, hardware, message set, intended layout, and references. Read it before any implementation work. This file does not restate the spec; it only covers how to *operate* in the repo.

## Status & operating constraints

- **All five phases work end to end, verified on hardware 2026-07-21.** `firmware/` is a working Zephyr **C++** app (see `notes/language-cpp.md`) running **two threads** across **four translation units**, one responsibility each: `sensor.cpp` (acquisition — reads the SCD-40 over I²C via the sensor API and publishes to a **zbus** channel), `protocol.cpp` (the wire format — the only place internal types meet nanopb/protobuf), `commands.cpp` (command semantics — dispatch, duplicate suppression, node identity), and `main.cpp` (the MQTT session — IPv4 on the on-board Ethernet, publish `Telemetry`, answer `Command`s with `Ack`s). The split is what makes the middle two testable without hardware. They meet on the channels in `firmware/src/app_channels.h` — read its header comment before touching either side. `proto/` holds the schema; `host/` holds the Python paho harness (`monitor.py`, `command.py`) — run it **on the Pi**, which is the only machine that can reach the broker. Verified against the bench: telemetry decode, all three commands, out-of-range rejection, duplicate suppression, a malformed payload that did not desync the stream, and a 66 s broker outage the sensor thread sampled straight through. The **schema-versioning exercise** is now done off-target: the `Telemetry` measurements are proto3 `optional` and `pressure_pa` was added for the peer node, and `tests/protocol/` hand-builds a descriptor for the previous schema with nanopb's `PB_BIND` X-macro so old↔new runs in both directions on qemu. The one thing it cannot show is the *dumb-gateway* payoff — changing the peer's schema and reflashing only the peer — which waits on the phase below.
- **A sixth phase is in progress: a second node over CAN.** A Nucleo-F072RB with a BME280 becomes a *peer node* with its own MQTT identity, and the H753ZI becomes a **gateway** that relays the peer's already-encoded Protobuf without decoding it. Be precise about what exists, because the halves are at very different stages:
  - **Built and verified:** on the H7, `CONFIG_CAN` + `CONFIG_CAN_SHELL` and an overlay `bitrate = <500000>`, exercised in **internal loopback only** — a frame sent and received through an RX filter with no transceiver attached.
  - **Built, and unverifiable without a peer:** the whole `sensor-node/` app for the F072RB — BME280 acquisition, the heartbeat, ISO-TP send and receive, command handling. It builds at 44 % flash / 55 % RAM and has never been run against another node, because a node alone on a bus cannot successfully transmit (nothing drives the ACK slot).
  - **Not built:** the gateway's `relay.cpp`, the relay zbus channels, and the second MQTT identity. `firmware/src/main.cpp` is still one session.
  - `firmware/src/can_link.h` is the **shared** link contract — address map, hand-packed heartbeat frame, the message-type byte above ISO-TP — included by both apps, with `tests/heartbeat/` covering it. `sensor-node/` compiles `firmware/src/protocol.cpp` and `commands.cpp` by relative path, so `firmware/src/` now has three consumers and `commands.h`'s `kNodeClientId` is an `extern` each app defines. Concepts in `notes/can-guide.md`, decisions and bring-up checks in `docs/can-bringup.md`. Two transceivers are still to be bought, so no real bus has ever carried a frame; do not write a bench procedure for one until it has actually been run.
- **Build / flash / console via the wrapper scripts**, not raw `west` — they source the workspace venv and pass `-s`/`-d` correctly. **All of them take `-a <app>` (or `APP=`) and default to `firmware`**; the board follows from the app, so `-b` is never needed:
  ```sh
  ./scripts/build.sh                   # incremental; -p forces pristine (required after devicetree/Kconfig edits)
  ./scripts/build.sh -a sensor-node -p # the F072RB peer node
  ./scripts/flash.sh -a sensor-node    # forces the openocd runner; the board's default runner isn't installed
  ./scripts/console.sh -a sensor-node  # serial console @115200 (quit with Ctrl-A then K, not Ctrl-A D)
  ./scripts/probe.sh                   # which ST-LINK and which /dev/cu.* belongs to which app
  ./scripts/cleanup.sh                 # remove build dirs; -a for one app
  ```
  **With both Nucleos attached, always say which one.** `console.sh` refuses to guess and prints `probe.sh`'s table; `flash.sh` resolves the app's probe through `scripts/probes.conf` and passes `--serial`, and **stops rather than flashing a board at random** if it cannot — losing that coin flip writes the wrong image to the wrong part and reports success. `STLINK_SERIAL=` overrides. The mapping is keyed on the ST-LINK serial, not the `usbmodemNNNNNN` name, which moves with the USB port. With one board attached none of this engages.
  The apps are **freestanding**: they build against a shared global west workspace at `~/zephyr-workspace` (**Zephyr v4.4.1**). Details in `docs/toolchain.md`.
- **Host-side tests exist; there is no lint tooling.** `./scripts/test.sh` runs 33 Ztest cases on `qemu_cortex_m3` in ~18 s with no board attached — `tests/protocol/` (the wire format), `tests/commands/` (dispatch, validator rejection, duplicate suppression) and `tests/heartbeat/` (the hand-packed CAN frame and address map). Run them before and after touching `protocol.cpp`, `commands.cpp` or `can_link.h` — all three are now compiled into more than one thing. Decisions and what is deliberately *not* covered are in `docs/test-strategy.md`; the concepts are in `notes/testing-guide.md`. Everything below the socket still needs the bench: verification there is build → flash → observe (console, `net` shell commands, or the Pi), and the **lab at the end of each guide** — `communication-guide.md` §9, `can-guide.md` §10, `zbus-guide.md` §10, `protobuf-guide.md` §12, `sensor-api-guide.md` §11, `zephyr-build-system-guide.md` §11, `language-cpp.md` §12, plus *Testing the Last Will* in `mqtt-design.md` and *Bring-up checks* in both `sensor-bringup.md` and `can-bringup.md`. **Write bench results as a procedure someone can re-run — commands, expected output, and what it proves — never as a dated report of a past run.** When behaviour changes, update the lab that covers it.
- **Networking is up:** static IPv4 `192.168.10.2/24`, no gateway, on a direct cable to the Pi at `192.168.10.1`. It is configured *entirely* in `firmware/prj.conf` via `CONFIG_NET_CONFIG_SETTINGS` — `net_config` applies it at boot, so no app code touches interface bring-up. Ping is verified both ways.
- **Decisions already made** (don't reopen without reason). In `docs/toolchain.md`: nanopb integration = the **in-tree module**; sensor driver = the **upstream in-tree `sensirion,scd40`**. In `docs/mqtt-design.md`: **QoS per topic + the topic hierarchy** (`node/<id>/{telemetry,command,ack,status}`; telemetry QoS 0, command/ack QoS 1, retained-will status). In `docs/sensor-bringup.md`: the sensor is **`zephyr,deferred-init`** and `sensor.cpp` initialises it, because a failed init is permanent and only its *timing* is controllable; and the **timed poll stays**, with the SCD-40's republish-on-fast-poll behaviour accepted and documented rather than worked around — read *Accepted limitation* there before touching `SAMPLE_PERIOD_MIN_MS` or believing a sub-5 s cadence. Record new decisions and their rationale in `docs/`.

## Working principles

- The deliverable is fluency in the patterns the README calls out — MQTT client lifecycle (connect/keepalive/reconnect, QoS, topics), zbus as the internal bus, schema versioning, and nanopb on a constrained target — not a polished product. Treat those as the real work.
- When a choice trades simplicity for fidelity to the real firmware↔SW-team contract, prefer fidelity. That is the point of the exercise.
- `proto/` is the single source of truth for the wire format; firmware and host both derive from it. Change the schema there and regenerate — never hand-edit generated code.
- **Documentation is split by kind, not by topic: guides live in `notes/`, references in `docs/`.** A topic normally has one of each, cross-linked both ways. `notes/README.md` is the index for the guides — reading order, per-guide prerequisites, and the shared skeleton every guide follows; keep it current when adding or renaming a guide:
  - **`notes/` — from-first-principles teaching guides.** General concepts, largely portable beyond this repo: `communication-guide.md`, `network-stack-guide.md`, `can-guide.md`, `zephyr-build-system-guide.md`, `protobuf-guide.md`, `zbus-guide.md`, `sensor-api-guide.md`, `language-cpp.md`, `shell-guide.md`, `testing-guide.md`.
  - **`docs/` — terse project references.** Decisions, rationale, verified facts, and what was actually built here: `mqtt-design.md`, `network-bringup.md`, `can-bringup.md`, `build-system-overview.md`, `sensor-bringup.md`, `test-strategy.md`, `toolchain.md`.
  - Pairings: `communication-guide` ↔ `mqtt-design`, `network-stack-guide` ↔ `network-bringup`, `can-guide` ↔ `can-bringup`, `zephyr-build-system-guide` ↔ `build-system-overview`, `sensor-api-guide` ↔ `sensor-bringup`, `testing-guide` ↔ `test-strategy`. `protobuf-guide.md`'s reference half is `proto/node.proto` itself, which carries the decisions inline; `zbus-guide.md`'s is the header comment in `firmware/src/app_channels.h`, same arrangement. `language-cpp.md` and `shell-guide.md` have no reference half.
  - **Exception:** `docs/firmware-mqtt-walkthrough.md` teaches, but it is a guided reading of *this repo's* `firmware/src/main.cpp` rather than a general concept, so it stays in `docs/` with the references — keep it in sync when the client changes.
  - Follow that split when adding a topic, and remember relative links cross the directory boundary (`../docs/…` from a guide, `../notes/…` from a reference). The user is learning these concepts, so **explain rather than assume** — don't take familiarity with a CLI, tool, or protocol for granted.
- Sensor access goes through Zephyr's sensor API, not raw I²C command codes. What the driver does underneath (command codes, conversion formulas, CRC-8 params) is documented inline where it matters in `notes/sensor-api-guide.md` and `docs/sensor-bringup.md` — reach for those when debugging the sensor path.
